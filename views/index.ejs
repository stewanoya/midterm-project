<!DOCTYPE html>
<html lang="en">
  
  <%- include('partials/_head') %>
  <head>
    <title>sQuiz Game</title>
  </head>

  <body>
    <%- include('partials/_header') %>

    <!-- utility div to provide spacing for fixed header -->
    <div class="spacer"></div>
    <div class="flex-container">
      <section class="jumbotron">
        <section class="call-to-action">
          <!-- might change color for contrast -->
          <h2 class="slogan">Welcome to sQuiz Game!</h2>
          <p class="slogan-text">
            The #1 way to test your knowledge on any subject. <br />
          </p>
          <br />
          <p class="slogan-text" style="margin-top: -1rem">
            Get started by creating your own Quiz, or check out our selection
            below!
          </p>
          <div class="button-container">
            <button class="btn-primary">Get Started</button>
            <button class="btn-primary search-button">Search Quizzes</button>
          </div>
        </section>
      </section>
    </div>

    <div class="flex-container icons-container">
      <div class="flex-container-column">
        <i class="fas fa-bolt fa-10x single-icon"></i>
        <h3 class="icon-title">Compete</h3>
        <p>
          Challenge your friends to do sQuizzes and see who gets the best
          result!
        </p>
      </div>
      <div class="flex-container-column">
        <i class="fas fa-brain fa-10x single-icon"></i>
        <h3 class="icon-title">Train</h3>
        <p>
          Train your mind by creating practice tests to help learn a topic of
          your choice!
        </p>
      </div>
      <div class="flex-container-column single-icon">
        <i class="fab fa-octopus-deploy fa-10x"></i>
        <h3 class="icon-title">Get sQuizzy!</h3>
        <p>Have fun with sQuiz Game!</p>
      </div>
    </div>

    <div class="flex-container">
      <section class="quiz-container">
        <h2 class="quiz-list-title">Featured sQuizzes</h2>
        <ul class="quiz-grid">
          <% for (quiz of quizzes) { %> <% if (quiz.ispublic == true) { %>
          <!-- replace anchor link with the link id url -->
          <a href="/quizzes/<%=quiz.short_url%>" method="GET">
            <li
              class="quiz-card"
              style="
                background-image: url('<%=quiz.cover_image_url%>');
                background-size: cover;
                background-position: center;
                background-repeat: no-repeat;
              "
            >
              <div class="quiz-category"><%=quiz.category%></div>
              <div class="quiz-title-container">
                <h3 class="quiz-title"><%=quiz.title%></h3>
              </div>
            </li>
          </a>
          <%}%> <% } %>
        </ul>
      </section>
    </div>
  </body>
</html>
